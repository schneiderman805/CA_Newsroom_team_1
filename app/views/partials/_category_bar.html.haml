.flex.justify-around.bg-red-dark.text-white.leading-loose.uppercase#category_bar
  - categories.each do |category|
    = link_to "#{category.name}", category_path(category), class: "text-white no-underline"